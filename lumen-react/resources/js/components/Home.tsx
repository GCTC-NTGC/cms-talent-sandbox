import * as React from 'react';

interface  HomeProps {
}

const Home: React.FunctionComponent< HomeProps> = (props) => {
  return (
      <section>
          <h1>Welcome!!!</h1>
      </section>
  );
};

export default Home;
